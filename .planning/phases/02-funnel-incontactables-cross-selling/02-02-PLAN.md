---
phase: 02-funnel-incontactables-cross-selling
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified: []
autonomous: false
requirements:
  - FUNNEL-01
  - FUNNEL-02
  - FUNNEL-03
  - FUNNEL-04
  - FUNNEL-05
  - FUNNEL-06
  - FUNNEL-07
  - FUNNEL-08
  - INCONT-01
  - INCONT-02
  - INCONT-03
  - CROSS-01
must_haves:
  truths:
    - "All 13 Embudo rows are visible in a single table with three segment columns (Total, Manufacturados, Individuales)"
    - "Every count row in Embudo shows a non-null CVR; Deals Cerrados CVR is non-zero and represents ratio vs Carry Over"
    - "Delta arrows (up/down/neutral) appear on every row in all three tables"
    - "Incontactables table shows Duplicado, Equivocado, SPAM rows with Mfg/Ind breakdown"
    - "Cross-Selling table shows Cross Sell Deals count with Mfg/Ind breakdown"
    - "All three tables refresh when user changes the date range and clicks Generar Reporte"
  artifacts:
    - path: "App.html"
      provides: "embudoRows, incontactablesRows, crossSellingRows computed arrays — already exist"
    - path: "Index.html"
      provides: "Embudo table (line 436), Incontactables table (line 574), Cross-Selling table (line 703) — already exist"
  key_links:
    - from: "Index.html embudoGeneral table (line 436)"
      to: "App.html embudoRows computed (line 180)"
      via: "v-for='row in embudoRows'"
      pattern: "embudoRows"
    - from: "Index.html incontactables table (line 574)"
      to: "App.html incontactablesRows computed (line 213)"
      via: "v-for='row in incontactablesRows'"
      pattern: "incontactablesRows"
    - from: "Index.html crossSelling table (line 703)"
      to: "App.html crossSellingRows computed (line 223)"
      via: "v-for='row in crossSellingRows'"
      pattern: "crossSellingRows"
---

<objective>
Deploy the CVR-fixed code and verify all three tables render correctly with real data. This is an audit + manual acceptance phase — the tables are already built. The objective is to confirm all 12 Phase 2 requirements are satisfied by examining the running app.

Purpose: Confirm Phase 2 requirements are closed. Identify any remaining rendering issues before marking Phase 2 complete.

Output: A signed-off acceptance checklist. No code changes expected; if a rendering issue is found, record it and fix it.
</objective>

<execution_context>
@/c/Users/juanc/Documents/anaconda_projects/Pedro - Dev/.planning/workflows/execute-plan.md
</execution_context>

<context>
@/c/Users/juanc/Documents/anaconda_projects/Pedro - Dev/.planning/PROJECT.md
@/c/Users/juanc/Documents/anaconda_projects/Pedro - Dev/.planning/ROADMAP.md
@/c/Users/juanc/Documents/anaconda_projects/Pedro - Dev/.planning/phases/02-funnel-incontactables-cross-selling/02-RESEARCH.md
@/c/Users/juanc/Documents/anaconda_projects/Pedro - Dev/.planning/phases/02-funnel-incontactables-cross-selling/02-01-SUMMARY.md

<interfaces>
<!-- Key sections already in Index.html for reference. No changes expected. -->

Index.html line 436: embudoGeneral table wrapper
  v-if="section.id === 'embudoGeneral'"
  v-for="row in embudoRows" — renders all 13 rows

Index.html line 574: incontactables table wrapper
  v-else-if="section.id === 'incontactables'"
  v-for="row in incontactablesRows" — renders 3 rows

Index.html line 703: crossSelling table wrapper
  v-else-if="section.id === 'crossSelling'"
  v-for="row in crossSellingRows" — renders 1 row

App.html getSegmentValue (line 304):
  Returns { count: 0, pct: '0.0', delta: '0.0' } on null/undefined — safe fallback

App.html formatAmount (line 309):
  Formats dollar amounts; returns '$0' if val is undefined — safe for amount rows
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy and do a static code audit of the three table templates</name>
  <files></files>
  <action>
    Run clasp push to deploy the fix from plan 02-01, then do a static verification of the three table templates to confirm they are structurally sound before browser testing.

    Step 1 — Deploy:
    ```
    clasp push
    ```

    Step 2 — Audit the CVR fix was applied:
    ```
    grep -n "prevCountRow" App.html
    grep -n "Con Interés" App.html
    ```
    Expected: prevCountRow appears on 2 lines; "Con Interés" appears on 1 line.

    Step 3 — Audit that the old buggy line is gone:
    ```
    grep -n "rawRows\[i - 1\]\.metric" App.html
    ```
    Expected: 0 results.

    Step 4 — Confirm all three computed arrays are exported in the return block:
    ```
    grep -n "embudoRows, incontactablesRows, crossSellingRows" App.html
    ```
    Expected: 1 line near line 2037.

    Step 5 — Confirm the three table sections exist in Index.html:
    ```
    grep -n "embudoGeneral\|incontactables\|crossSelling" Index.html | grep "section.id"
    ```
    Expected: 3 lines — one for each section id check.

    If any of these grep checks fail, fix the problem before proceeding to the browser checkpoint.
  </action>
  <verify>
    <automated>grep -n "prevCountRow" App.html && grep -c "Con Interés" App.html && grep -c "rawRows\[i - 1\]\.metric" App.html</automated>
  </verify>
  <done>
    All static checks pass:
    - prevCountRow: 2 hits
    - Con Interés: 1 hit
    - Old buggy rawRows[i - 1] pattern: 0 hits
    - clasp push completed without error
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 2 tables: Embudo General (13 rows), Incontactables (3 rows), Cross-Selling (1 row). CVR bug for Deals Cerrados was fixed in plan 02-01. All tables were already implemented before Phase 2 began — this checkpoint confirms they render correctly with real data.
  </what-built>
  <how-to-verify>
    1. Open the GAS web app in a browser.
    2. Navigate to the "Reportes" view (ADMIN or GERENTE role required).
    3. Set a date range that covers a period with data and click "Generar Reporte".
    4. Walk through this checklist:

    **Embudo General table (FUNNEL-01 through FUNNEL-08):**
    - [ ] Table shows exactly 13 rows: Total Leads, Contactables, Contactados, Con Respuesta, Dialogo Completo, Dialogo Intermitente, Con Interés (with accent), Descartados, Asignados a Ventas, Carry Over, Monto Inversion, Deals Cerrados, Monto Cierres
    - [ ] Each count row has 3 segment columns: Total / Manufacturados / Individuales
    - [ ] Monto Inversion and Monto Cierres show `$` formatted amounts (e.g., `$125,000`) not a raw count
    - [ ] Every count row (not amount) has a CVR% value next to the label
    - [ ] "Deals Cerrados" CVR is NOT 0.0% (it should show a non-zero ratio vs Carry Over count)
    - [ ] Every row shows an up arrow / down arrow / neutral dash delta indicator with a percentage
    - [ ] Mfg/Ind columns are populated (not all zeros unless data is actually zero)

    **Incontactables table (INCONT-01, INCONT-02, INCONT-03):**
    - [ ] Table shows 3 rows: Duplicado, Equivocado, SPAM
    - [ ] Duplicado and Equivocado show counts with Mfg/Ind breakdown and delta arrows
    - [ ] SPAM shows all zeros (this is expected — no SPAM data source in the database)

    **Cross-Selling table (CROSS-01):**
    - [ ] Table shows 1 row: Cross Sell Deals
    - [ ] Count shown with Mfg/Ind breakdown and delta arrow

    **Reactivity (PERIOD-03 — regression check):**
    - [ ] Change the date range and click "Generar Reporte" again — all three tables update

    **Known acceptable behaviors:**
    - SPAM row showing 0/0/0 is by design (no SPAM status in fact_leads)
    - Carry Over CVR vs Asignados a Ventas may be >100% — this is valid business data
    - Amount rows show `--` in the percentage column — this is correct
  </how-to-verify>
  <resume-signal>
    Type "approved" if all checked items pass.
    Or describe any failing items specifically (e.g., "Deals Cerrados CVR still 0.0%" or "Monto Cierres delta arrow missing") — the executor will fix and re-verify.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 2 is complete when:
1. Static grep checks all pass (Task 1)
2. Human approves the checkpoint checklist (Task 2)
3. All 12 requirements confirmed rendered: FUNNEL-01..08, INCONT-01..03, CROSS-01
</verification>

<success_criteria>
- All 13 Embudo rows visible with correct labels (including "Con Interés" with accent)
- Deals Cerrados CVR is non-zero and represents ratio vs Carry Over
- Delta arrows appear on all rows across all three tables
- Incontactables and Cross-Selling tables render with Mfg/Ind breakdowns
- All tables update when period selector changes
- Human tester types "approved" at the checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/02-funnel-incontactables-cross-selling/02-02-SUMMARY.md` with:
- Requirements verified: FUNNEL-01 through FUNNEL-08, INCONT-01 through INCONT-03, CROSS-01
- Tables audited: Embudo General (13 rows), Incontactables (3 rows), Cross-Selling (1 row)
- Known data behavior documented: SPAM always zero (no source in DB)
- Phase 2 status: complete
</output>
