---
phase: 06-visual-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Analytics.js
  - Styles.html
  - Index.html
autonomous: true
requirements:
  - VELOCITY-01
  - ALERT-01

must_haves:
  truths:
    - "calculateDealsReport_() returns avgDaysToClose (current) and prevAvgDaysToClose in its result object"
    - "avgDaysToClose is 0 when no fondeo=true deals exist in the period (safe empty-array handling)"
    - "Every delta span across ALL report sections gains CSS class delta-alert when |delta| >= 20"
    - "delta-alert styling is visually distinct (bold background highlight, not just a different text color)"
  artifacts:
    - path: "Analytics.js"
      provides: "avgDaysToClose + prevAvgDaysToClose fields added to calculateDealsReport_() return"
      contains: "avgDaysToClose"
    - path: "Styles.html"
      provides: ".delta-alert CSS rule with distinct background-color highlight"
      contains: "delta-alert"
    - path: "Index.html"
      provides: "v-bind:class delta-alert applied to all existing delta spans"
      contains: "delta-alert"
  key_links:
    - from: "Analytics.js calculateDealsReport_()"
      to: "App.html dealsReportRows computed"
      via: "reportData.value.dealsReport.avgDaysToClose"
      pattern: "avgDaysToClose"
    - from: "Styles.html .delta-alert"
      to: "Index.html delta spans"
      via: "v-bind:class with |delta| >= 20 threshold"
      pattern: "delta-alert"
---

<objective>
Add avgDaysToClose velocity metric to the Deals backend and wire global delta-alert highlighting to all existing delta cells.

Purpose: VELOCITY-01 requires showing deal close speed in the Deals section; ALERT-01 requires all delta variations >= ±20% to be visually distinctive across the entire report.
Output: Analytics.js updated with velocity calc; Styles.html with delta-alert CSS rule; Index.html with delta-alert class binding on every delta span.
</objective>

<execution_context>
@C:/Users/juanc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/juanc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Key patterns from existing codebase. Use directly — no exploration needed. -->

From Analytics.js calculateDealsReport_() (lines 1008–1113):
- Filters currentDeals and previousDeals by fecha_pase_ventas within period range
- currentDeals loop at line 1045 iterates cd.fondeo, cd.cotizo, etc.
- Return object at line 1107: { totalDeals, stages, montoCotizacion, montoCierre, razonesPerdida }
- ES5 ONLY: use var, for loops, no arrow functions, no const/let

From Analytics.js calcDelta_() (lines 101–105):
```javascript
function calcDelta_(currentVal, previousVal) {
  if (previousVal === 0 && currentVal > 0) return '100.0';
  if (previousVal === 0 && currentVal === 0) return '0.0';
  return (((currentVal - previousVal) / previousVal) * 100).toFixed(1);
}
```

From Index.html delta span pattern (lines 486–497) — pattern repeated ~20x across all sections:
```html
<span v-if="Number(getSegmentValue(row.metric, 'total').delta) > 0" class="delta-positive">
<span v-else-if="Number(getSegmentValue(row.metric, 'total').delta) < 0" class="delta-negative">
<span v-else class="delta-neutral">
```
Also plain delta pattern (lines 1368–1379):
```html
<span v-if="Number(row.delta) > 0" class="delta-positive">
<span v-else-if="Number(row.delta) < 0" class="delta-negative">
<span v-else class="delta-neutral">
```

From Styles.html delta classes (lines 1691–1716):
```css
.delta-positive { color: var(--accent-success); display: inline-flex; align-items: center; gap: 2px; font-weight: 500; font-size: 0.78rem; }
.delta-negative { color: var(--accent-danger); ... }
.delta-neutral  { color: var(--text-muted); ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add avgDaysToClose to calculateDealsReport_()</name>
  <files>Analytics.js</files>
  <action>
    Inside calculateDealsReport_() in Analytics.js (ES5 only — var, for-loops, no const/let/arrow functions):

    After the existing Step 4 amounts loop (around line 1056), add a new Step 4b that computes avgDaysToClose for current period:

    ```javascript
    // Step 4b — average days to close (fondeo=true deals only)
    var today = new Date();
    var closedDaysSum = 0;
    var closedCount = 0;
    for (var v = 0; v < currentDeals.length; v++) {
      var vd = currentDeals[v];
      if (vd.fondeo === true) {
        var startMs = new Date(vd.fecha_pase_ventas || '').getTime();
        if (!isNaN(startMs)) {
          closedDaysSum += Math.round((today.getTime() - startMs) / 86400000);
          closedCount++;
        }
      }
    }
    var avgDaysToClose = closedCount > 0 ? Math.round(closedDaysSum / closedCount) : 0;
    ```

    Add the same loop for previousDeals to compute prevAvgDaysToClose:

    ```javascript
    var prevClosedDaysSum = 0;
    var prevClosedCount = 0;
    for (var pv = 0; pv < previousDeals.length; pv++) {
      var pvd = previousDeals[pv];
      if (pvd.fondeo === true) {
        var pstartMs = new Date(pvd.fecha_pase_ventas || '').getTime();
        if (!isNaN(pstartMs)) {
          prevClosedDaysSum += Math.round((today.getTime() - pstartMs) / 86400000);
          prevClosedCount++;
        }
      }
    }
    var prevAvgDaysToClose = prevClosedCount > 0 ? Math.round(prevClosedDaysSum / prevClosedCount) : 0;
    ```

    Update the return object at the end of calculateDealsReport_() to include the new fields:

    ```javascript
    return {
      totalDeals:        total,
      stages:            stages,
      montoCotizacion:   montoCotizacion,
      montoCierre:       montoCierre,
      razonesPerdida:    razonesPerdida,
      avgDaysToClose:    avgDaysToClose,
      prevAvgDaysToClose: prevAvgDaysToClose
    };
    ```

    Do NOT modify any other function. Do NOT use let/const/arrow functions — ES5 only.
  </action>
  <verify>
    <automated>grep -n "avgDaysToClose" "C:/Users/juanc/Documents/anaconda_projects/Pedro - Dev/Analytics.js"</automated>
  </verify>
  <done>Analytics.js contains avgDaysToClose and prevAvgDaysToClose in both the computation block and the return object of calculateDealsReport_(). Empty-array case returns 0 (not NaN) due to closedCount guard.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add .delta-alert CSS rule to Styles.html</name>
  <files>Styles.html</files>
  <action>
    In Styles.html, locate the "Delta% Column" CSS block (around line 1685). After the .delta-neutral rule block (around line 1716), add the new .delta-alert rule. It must be applied ON TOP of delta-positive or delta-negative (it will be added as an additional class), so it overrides background only:

    ```css
    /* ── Delta Alert: |delta| >= 20% ── */
    .delta-alert {
        background-color: rgba(255, 193, 7, 0.18);
        border-radius: 4px;
        padding: 1px 4px;
        font-weight: 700;
    }
    ```

    This gives a distinct amber highlight background. The .delta-positive/.delta-negative text colors already convey direction; .delta-alert adds the background highlight for emphasis at threshold crossings. Do NOT remove or change any existing delta CSS rule.
  </action>
  <verify>
    <automated>grep -n "delta-alert" "C:/Users/juanc/Documents/anaconda_projects/Pedro - Dev/Styles.html"</automated>
  </verify>
  <done>Styles.html contains a .delta-alert CSS rule with background-color and font-weight: 700 after the existing delta class block.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Apply delta-alert class binding to ALL delta spans in Index.html</name>
  <files>Index.html</files>
  <action>
    In Index.html there are two types of delta span patterns. Apply delta-alert to each by adding v-bind:class (or merging into existing class binding).

    **Pattern A — segmented delta spans (getSegmentValue):**
    Every `<span ... class="delta-positive">` / `class="delta-negative"` / `class="delta-neutral"` that reads from `getSegmentValue(row.metric, segment).delta` must become a v-bind:class that also conditionally adds `delta-alert` when |delta| >= 20.

    Replace each occurrence of the three-span group. The delta value comes from `getSegmentValue(row.metric, 'total').delta` (or 'manufacturers' / 'individuals'). Use this helper expression inline:

    For delta-positive spans:
    ```html
    <span v-if="Number(getSegmentValue(row.metric, 'total').delta) > 0"
      :class="['delta-positive', Math.abs(Number(getSegmentValue(row.metric, 'total').delta)) >= 20 ? 'delta-alert' : '']">
    ```
    For delta-negative spans:
    ```html
    <span v-else-if="Number(getSegmentValue(row.metric, 'total').delta) < 0"
      :class="['delta-negative', Math.abs(Number(getSegmentValue(row.metric, 'total').delta)) >= 20 ? 'delta-alert' : '']">
    ```
    For delta-neutral spans — no alert needed (delta is 0, |0| < 20), keep as static class:
    ```html
    <span v-else class="delta-neutral">
    ```

    Apply this to ALL segmented delta columns: total, manufacturers, individuals — in ALL report sections (Embudo, Incontactables, Cross-Selling, Toques/Semaforo, Razones No Paso, Razones Perdio sections).

    **Pattern B — plain delta spans (row.delta used directly, e.g. Razones and DealsLoss sections):**
    ```html
    <span v-if="Number(row.delta) > 0"
      :class="['delta-positive', Math.abs(Number(row.delta)) >= 20 ? 'delta-alert' : '']">
    <span v-else-if="Number(row.delta) < 0"
      :class="['delta-negative', Math.abs(Number(row.delta)) >= 20 ? 'delta-alert' : '']">
    <span v-else class="delta-neutral">
    ```

    **Semaforo delta spans** (lines ~898–963, use `getSegmentValue(toque, semaforoSegment).delta`):
    ```html
    <span v-if="Number(getSegmentValue(toque, semaforoSegment).delta) > 0"
      :class="['delta-positive', Math.abs(Number(getSegmentValue(toque, semaforoSegment).delta)) >= 20 ? 'delta-alert' : '']"
      style="font-size:0.7rem;">
    <span v-else-if="Number(getSegmentValue(toque, semaforoSegment).delta) < 0"
      :class="['delta-negative', Math.abs(Number(getSegmentValue(toque, semaforoSegment).delta)) >= 20 ? 'delta-alert' : '']"
      style="font-size:0.7rem;">
    ```

    Work through Index.html top-to-bottom and update every single delta-positive and delta-negative span. The delta-neutral span never needs delta-alert (value is 0). Do NOT skip any section. Verify count: there should be 0 remaining occurrences of `class="delta-positive"` or `class="delta-negative"` (all converted to :class bindings).
  </action>
  <verify>
    <automated>grep -c "class=\"delta-positive\"\|class=\"delta-negative\"" "C:/Users/juanc/Documents/anaconda_projects/Pedro - Dev/Index.html"</automated>
  </verify>
  <done>Zero occurrences of static `class="delta-positive"` or `class="delta-negative"` remain in Index.html. All delta spans use :class array binding with delta-alert conditional. grep for "delta-alert" in Index.html returns >= 20 matches.</done>
</task>

</tasks>

<verification>
After all three tasks complete:

1. `grep -n "avgDaysToClose" Analytics.js` — shows at least 3 lines (computation + prev + return)
2. `grep -n "delta-alert" Styles.html` — shows the .delta-alert CSS rule
3. `grep -c "delta-alert" Index.html` — shows >= 20 matches (one per delta-positive/negative span)
4. `grep -c "class=\"delta-positive\"" Index.html` — should return 0 (all converted to :class bindings)
5. `grep -c "closedCount > 0" Analytics.js` — shows the safe empty-array guard is present
</verification>

<success_criteria>
- calculateDealsReport_() returns avgDaysToClose and prevAvgDaysToClose; returns 0 (not NaN) when no fondeo deals exist
- Styles.html has .delta-alert rule with visible background-color amber highlight
- Index.html has delta-alert applied to delta-positive and delta-negative spans in ALL sections via :class array binding
- No static class="delta-positive" or class="delta-negative" strings remain (all use :class)
- ES5 constraint maintained: no let/const/arrow functions added to Analytics.js
</success_criteria>

<output>
After completion, create `.planning/phases/06-visual-intelligence/06-01-SUMMARY.md`
</output>
