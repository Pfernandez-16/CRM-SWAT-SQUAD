---
phase: 04-deals-backend-+-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Analytics.js
autonomous: false
requirements:
  - DEALS-01
  - DEALS-02

must_haves:
  truths:
    - "fact_deals Google Sheet has five new boolean columns: cotizo, en_negociacion, asistio_demo, firmo_contrato, fondeo"
    - "Existing fact_deals rows have FALSE defaults in all five new columns"
    - "calculateDealsReport_() exists in Analytics.js and returns a dealsReport object with stages and razonesPerdida"
    - "getSDRReport() return object includes a dealsReport key with the calculateDealsReport_() result"
  artifacts:
    - path: "Analytics.js"
      provides: "calculateDealsReport_() function + dealsReport in getSDRReport return"
      contains: "calculateDealsReport_"
    - path: "fact_deals (Google Sheet)"
      provides: "Five new boolean funnel columns"
      contains: "cotizo, en_negociacion, asistio_demo, firmo_contrato, fondeo headers"
  key_links:
    - from: "getSDRReport()"
      to: "calculateDealsReport_()"
      via: "function call added after matrizContactabilidad call"
      pattern: "calculateDealsReport_"
    - from: "calculateDealsReport_()"
      to: "fact_deals sheet"
      via: "allDeals array already loaded via readTable_(T_DEALS)"
      pattern: "deal\\.cotizo|deal\\.fondeo"
---

<objective>
Add five boolean funnel columns to the fact_deals Google Sheet and implement calculateDealsReport_() in Analytics.js. Wire the new function into getSDRReport()'s return envelope.

Purpose: Deals data has no funnel boolean fields — cotizo/en_negociacion/asistio_demo/firmo_contrato/fondeo must exist before the frontend can show Si/No counts per stage. The backend function must deliver those counts plus monto_cotizacion and monto_cierre aggregates and razon_perdida breakdowns.
Output: Analytics.js with calculateDealsReport_() defined and dealsReport key present in getSDRReport() result. fact_deals sheet updated with 5 new columns.
</objective>

<execution_context>
@C:/Users/juanc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/juanc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- From Analytics.js — getSDRReport return structure (lines 1068-1088) -->
<!-- Add dealsReport key alongside the existing 9 sections -->
var result = {
  metadata: { ... },
  embudoGeneral: embudoGeneral,
  incontactables: incontactables,
  crossSelling: crossSelling,
  semaforoContesto: semaforoContesto,
  semaforoNoContesto: semaforoNoContesto,
  sinRespuesta6toToque: sinRespuesta6toToque,
  razonesNoPasoVentas: razonesNoPasoVentas,
  razonesPerdioVenta: razonesPerdioVenta,
  matrizContactabilidad: matrizContactabilidad
  // NEW: dealsReport: dealsReport
};

<!-- From Código.js — readTable_ lowercases all header keys -->
// readTable_ lowercases headers: deal.cotizo will be TRUE/FALSE (Sheets checkbox)

<!-- Existing fact_deals columns (from CSV header) -->
// id_deal, id_lead, id_contacto, id_vendedor_ae, id_producto, fecha_pase_ventas,
// ventas_contacto_cliente, numero_toque_ae, toque_ae, fecha_primer_contacto_ae,
// status_venta, proyeccion, monto_cotizacion, monto_apartado, monto_cierre,
// fecha_reagenda, hora_reagenda, fecha_cierre, razon_perdida, descuento_pct,
// ...notas_vendedor
// NEW COLUMNS TO ADD: cotizo, en_negociacion, asistio_demo, firmo_contrato, fondeo

<!-- ES5 constraint: var only, function declarations, no arrow functions,
     no const/let, no template literals. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Add five boolean columns to fact_deals Google Sheet</name>
  <files>fact_deals (Google Sheet — not a local file)</files>
  <action>
    Open the fact_deals tab in the Google Sheet attached to the GAS project
    (Script ID: 1AT7UA0NAuNTvRP08_tpBKCkqXM9PtAio3AsrA7dO1f1v7Uxe90XxL5uU).

    Add five new columns to the RIGHT of the existing last column (notas_vendedor).
    Headers must be exactly (lowercase, underscores):

      cotizo | en_negociacion | asistio_demo | firmo_contrato | fondeo

    For each new column:
    1. Type the header name in row 1.
    2. Select all data rows below (row 2 to last row with data).
    3. Insert checkboxes: Format > Cells > Insert checkboxes — sets existing rows to FALSE.

    Verify:
    - Row 1 ends with: cotizo | en_negociacion | asistio_demo | firmo_contrato | fondeo
    - Existing data rows show unchecked checkboxes (FALSE) in all five columns.
  </action>
  <verify>Open the sheet and visually confirm five new checkbox columns with the correct header names in row 1.</verify>
  <done>fact_deals sheet has 5 new checkbox columns; existing rows default to FALSE</done>
  <resume-signal>Type "columns added" when the five checkbox columns are in place</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Implement calculateDealsReport_() and wire into getSDRReport()</name>
  <files>Analytics.js</files>
  <action>
    Add calculateDealsReport_() immediately before getSDRReport() (before line 1005).
    Then add the call and return key inside getSDRReport().

    CRITICAL: ES5 only — var, function declarations, no arrow functions, no const/let, no template literals.

    Function signature:
      function calculateDealsReport_(allDeals, dateIn, dateOut, prevDateIn, prevDateOut)

    LOGIC:

    Step 1 — Date-filter allDeals to current period using fecha_pase_ventas:
      Use new Date(deal.fecha_pase_ventas).getTime() and compare against
      new Date(dateIn).getTime() and new Date(dateOut).getTime() (inclusive).
      Skip deals where fecha_pase_ventas is empty or NaN.

    Step 2 — Date-filter allDeals to previous period the same way using prevDateIn/prevDateOut.

    Step 3 — Count Si/No per stage for currentDeals:
      - contactado:     si = currentDeals.length, no = 0 (all deals in fact_deals were contacted)
      - cotizado:       si = count where deal.cotizo is truthy, no = rest
      - en_negociacion: si = count where deal.en_negociacion is truthy, no = rest
      - asistio_demo:   si = count where deal.asistio_demo is truthy, no = rest
      - firmo_contrato: si = count where deal.firmo_contrato is truthy, no = rest
      - fondeo:         si = count where deal.fondeo is truthy, no = rest

    Step 4 — Amounts:
      montoCotizacion: sum Number(deal.monto_cotizacion)||0 for currentDeals where deal.cotizo is truthy
      montoCierre: sum Number(deal.monto_cierre)||0 for currentDeals where deal.firmo_contrato is truthy

    Step 5 — Loss reasons:
      For currentDeals where String(deal.status_venta||'').trim() === 'Perdido':
        Count occurrences of deal.razon_perdida string value.
      For previousDeals, same logic.
      Build razonesPerdida object keyed by reason string:
        { current: N, previous: M, delta: calcDelta_(N, M) }
      Use calcDelta_() which is already defined in Analytics.js.

    Return object shape:
    {
      totalDeals: currentDeals.length,
      stages: {
        contactado:     { si: N, no: 0 },
        cotizado:       { si: N, no: N },
        en_negociacion: { si: N, no: N },
        asistio_demo:   { si: N, no: N },
        firmo_contrato: { si: N, no: N },
        fondeo:         { si: N, no: N }
      },
      montoCotizacion: N,
      montoCierre: N,
      razonesPerdida: {
        'reason string': { current: N, previous: M, delta: '0.0' },
        ...
      }
    }

    Inside getSDRReport(), add after the matrizContactabilidad call (line ~1065):
      var dealsReport = calculateDealsReport_(allDeals, dateIn, dateOut, prevDateIn, prevDateOut);

    In the result object, after "matrizContactabilidad: matrizContactabilidad":
      , dealsReport: dealsReport

    Do NOT alter any other line of getSDRReport().
  </action>
  <verify>
    <automated>grep -n "calculateDealsReport_\|dealsReport" Analytics.js</automated>
  </verify>
  <done>
    - calculateDealsReport_() defined in Analytics.js in ES5 syntax (var/function only)
    - getSDRReport() calls calculateDealsReport_() and includes dealsReport in its return object
    - Function returns { totalDeals, stages, montoCotizacion, montoCierre, razonesPerdida }
    - Empty allDeals array returns zero-count object without throwing
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
  grep -c "calculateDealsReport_" Analytics.js
  # Must be >= 3: function definition + call in getSDRReport + at least 1 usage

  grep -n "dealsReport:" Analytics.js
  # Must appear in the result object (1 line)

  grep -n "var dealsReport" Analytics.js
  # Must appear as the call site (1 line)
</verification>

<success_criteria>
- fact_deals sheet has 5 new checkbox columns: cotizo, en_negociacion, asistio_demo, firmo_contrato, fondeo
- Existing rows default to FALSE (unchecked) in all five columns
- calculateDealsReport_() is defined in Analytics.js using ES5 syntax only
- getSDRReport() returns dealsReport key in its JSON envelope
- No regression to existing 9 sections in getSDRReport() return object
</success_criteria>

<output>
After completion, create `.planning/phases/04-deals-backend-+-frontend/04-01-SUMMARY.md`
</output>
