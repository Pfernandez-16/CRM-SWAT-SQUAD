---
phase: 04-deals-backend-+-frontend
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - App.html
  - Index.html
autonomous: true
requirements:
  - DEALS-03
  - DEALS-04
  - DEALS-05

must_haves:
  truths:
    - "reportSections array includes a deals entry so the accordion section renders"
    - "dealsReportRows computed in App.html maps reportData.dealsReport into stage rows with si/no counts and amounts"
    - "dealsLossRows computed in App.html maps reportData.dealsReport.razonesPerdida into reason rows with counts and delta"
    - "Index.html has a v-else-if block for section.id === 'deals' rendering the funnel table"
    - "Index.html has a v-else-if block for section.id === 'dealsLoss' rendering the loss-reason table"
    - "Both new computeds are included in the setup() return object"
  artifacts:
    - path: "App.html"
      provides: "dealsReportRows and dealsLossRows computeds + reportSections entries"
      contains: "dealsReportRows"
    - path: "Index.html"
      provides: "Deals funnel table + Deals loss-reason table markup"
      contains: "section.id === 'deals'"
  key_links:
    - from: "dealsReportRows computed"
      to: "reportData.value.dealsReport.stages"
      via: "computed(() => { if (!reportData.value || !reportData.value.dealsReport) return []; })"
      pattern: "dealsReport\\.stages"
    - from: "Index.html deals table"
      to: "dealsReportRows"
      via: "v-for='row in dealsReportRows'"
      pattern: "v-for.*dealsReportRows"
    - from: "Index.html dealsLoss table"
      to: "dealsLossRows"
      via: "v-for='row in dealsLossRows'"
      pattern: "v-for.*dealsLossRows"
---

<objective>
Add the Deals/Negociaciones frontend: two new report sections (funnel table and loss-reason table) wired to the dealsReport key that 04-01 added to getSDRReport()'s response.

Purpose: The backend now returns dealsReport data; this plan makes it visible to the user as a Deals funnel table (Contactado through Fondeo with Si/No counts and monto columns) and a Deals loss-reason table with delta.
Output: App.html updated with dealsReportRows + dealsLossRows computeds; Index.html updated with two new v-else-if table blocks; both sections appear in the accordion.
</objective>

<execution_context>
@C:/Users/juanc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/juanc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04-deals-backend-+-frontend/04-01-SUMMARY.md

<interfaces>
<!-- From App.html setup() — existing report data access pattern -->
<!-- reportData is ref(null); populated by generateReport() via getSDRReport() -->
<!-- All computeds follow this guard pattern: -->
const someRows = computed(() => {
    if (!reportData.value || !reportData.value.sectionKey) return [];
    const section = reportData.value.sectionKey;
    return [ { label: 'Row Label', ... } ];
});

<!-- dealsReport shape returned by calculateDealsReport_() (from 04-01) -->
{
  totalDeals: N,
  stages: {
    contactado:    { si: N, no: 0 },
    cotizado:      { si: N, no: N },
    en_negociacion:{ si: N, no: N },
    asistio_demo:  { si: N, no: N },
    firmo_contrato:{ si: N, no: N },
    fondeo:        { si: N, no: N }
  },
  montoCotizacion: N,
  montoCierre:     N,
  razonesPerdida: {
    'reason string': { current: N, previous: M, delta: '0.0' },
    ...
  }
}

<!-- reportSections array in App.html (lines 165-175) — add two new entries at the END -->
const reportSections = [
    { id: 'embudoGeneral',         label: 'Embudo General' },
    { id: 'incontactables',        label: 'Incontactables' },
    { id: 'crossSelling',          label: 'Cross Selling' },
    { id: 'matrizContactabilidad', label: 'Matriz de Contactabilidad' },
    { id: 'semaforoContesto',      label: 'Semaforo Contesto' },
    { id: 'semaforoNoContesto',    label: 'Semaforo No Contesto' },
    { id: 'sinRespuesta',          label: 'Sin Respuesta 6to Toque' },
    { id: 'noPasoVentas',          label: 'Por que no paso a Ventas' },
    { id: 'perdioVenta',           label: 'Por que perdio la venta' }
    // ADD:
    // { id: 'deals',     label: 'Deals / Negociaciones' },
    // { id: 'dealsLoss', label: 'Deals - Razones de Perdida' }
];

<!-- setup() return statement (lines 2021-2081) — Reportes section at line 2039-2046 -->
// ADD dealsReportRows, dealsLossRows after razonesPerdioRows in the return

<!-- Index.html — insertion point for new sections -->
<!-- Line 1322 closes the perdioVenta div -->
<!-- Line 1324: <p v-else ...>Seccion en construccion...</p> -->
<!-- Insert two new v-else-if blocks between line 1322 and line 1324 -->

<!-- Existing table pattern reference (razones table, Index.html ~line 1205) -->
<div v-else-if="section.id === 'perdioVenta'" class="report-table-wrapper">
  <table class="report-table">
    <thead>...</thead>
    <tbody>
      <tr v-for="row in razonesPerdioRows" :key="row.label">...</tr>
    </tbody>
  </table>
</div>

<!-- Existing delta pattern in Index.html -->
<span v-if="Number(row.delta) > 0" class="delta-positive">
  <span class="material-symbols-outlined">arrow_upward</span>
  +{{ row.delta }}%
</span>
<span v-else-if="Number(row.delta) < 0" class="delta-negative">
  <span class="material-symbols-outlined">arrow_downward</span>
  {{ row.delta }}%
</span>
<span v-else class="delta-neutral">
  <span class="material-symbols-outlined">remove</span>
  0%
</span>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dealsReportRows and dealsLossRows computeds + update reportSections and return</name>
  <files>App.html</files>
  <action>
    Make four surgical edits to App.html:

    EDIT A — reportSections array (around line 174, after the perdioVenta entry):
    Add two new entries at the END of the reportSections array, before the closing bracket:
      { id: 'deals', label: 'Deals / Negociaciones' },
      { id: 'dealsLoss', label: 'Deals - Razones de Perdida' }

    EDIT B — New computed: dealsReportRows
    Insert immediately AFTER the razonesPerdioRows computed block (after its closing }); — around line 305):

        const dealsReportRows = computed(() => {
            if (!reportData.value || !reportData.value.dealsReport) return [];
            const dr = reportData.value.dealsReport;
            const s = dr.stages || {};
            return [
                { label: 'Contactado',     stage: s.contactado,     type: 'count', monto: null },
                { label: 'Cotizado',       stage: s.cotizado,       type: 'count', monto: dr.montoCotizacion },
                { label: 'En Negociacion', stage: s.en_negociacion, type: 'count', monto: null },
                { label: 'Asistio Demo',   stage: s.asistio_demo,   type: 'count', monto: null },
                { label: 'Firmo Contrato', stage: s.firmo_contrato, type: 'count', monto: null },
                { label: 'Fondeo',         stage: s.fondeo,         type: 'count', monto: dr.montoCierre }
            ];
        });

    EDIT C — New computed: dealsLossRows
    Insert immediately AFTER dealsReportRows:

        const dealsLossRows = computed(() => {
            if (!reportData.value || !reportData.value.dealsReport) return [];
            const rp = reportData.value.dealsReport.razonesPerdida || {};
            return Object.keys(rp).map(function(key) {
                return {
                    label: key || 'Sin especificar',
                    current: rp[key].current || 0,
                    previous: rp[key].previous || 0,
                    delta: rp[key].delta || '0.0'
                };
            });
        });

    EDIT D — Return statement (around line 2044, the Reportes section of the return):
    Find the line:  sinRespuestaRows, razonesNoPasoRows, razonesPerdioRows,
    Change it to:   sinRespuestaRows, razonesNoPasoRows, razonesPerdioRows, dealsReportRows, dealsLossRows,
  </action>
  <verify>
    <automated>
      grep -n "dealsReportRows\|dealsLossRows" "/c/Users/juanc/Documents/anaconda_projects/Pedro - Dev/App.html"
      # Expect: computed definition lines + return statement line (3+ matches each)

      grep -n "id: 'deals'" "/c/Users/juanc/Documents/anaconda_projects/Pedro - Dev/App.html"
      # Expect: 2 lines (deals entry + dealsLoss entry in reportSections)
    </automated>
  </verify>
  <done>
    - reportSections has deals and dealsLoss entries
    - dealsReportRows computed defined and guards against missing reportData.dealsReport
    - dealsLossRows computed defined using Object.keys() iteration
    - Both computeds exported in setup() return object
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Deals funnel table and Deals loss-reason table to Index.html</name>
  <files>Index.html</files>
  <action>
    Insert two new v-else-if blocks in Index.html BETWEEN the closing tag of the perdioVenta block
    (line 1322: closing </div>) and the fallback v-else paragraph (line 1324:
    "<p v-else style=...>Seccion en construccion...</p>").

    INSERT BLOCK 1 — Deals funnel table (section.id === 'deals'):

                  <!-- Deals / Negociaciones Funnel Table -->
                  <div v-else-if="section.id === 'deals'" class="report-table-wrapper">
                    <table class="report-table">
                      <thead>
                        <tr>
                          <th style="min-width:160px;">Etapa</th>
                          <th>Si</th>
                          <th>No</th>
                          <th data-tooltip="Monto donde aplica (Cotizacion o Cierre)">Monto</th>
                        </tr>
                      </thead>
                      <tbody>
                        <tr v-for="row in dealsReportRows" :key="row.label">
                          <td class="col-metric">{{ row.label }}</td>
                          <td class="col-count">{{ (row.stage && row.stage.si) || 0 }}</td>
                          <td class="col-count">{{ (row.stage && row.stage.no) || 0 }}</td>
                          <td class="col-count">
                            <span v-if="row.monto !== null && row.monto !== undefined">
                              {{ formatAmount(row.monto) }}
                            </span>
                            <span v-else style="color:var(--text-muted);">--</span>
                          </td>
                        </tr>
                      </tbody>
                    </table>
                  </div>

    INSERT BLOCK 2 — Deals loss-reason table (section.id === 'dealsLoss'):

                  <!-- Deals - Razones de Perdida Table -->
                  <div v-else-if="section.id === 'dealsLoss'" class="report-table-wrapper">
                    <table class="report-table">
                      <thead>
                        <tr>
                          <th style="min-width:160px;">Razon de Perdida</th>
                          <th>Periodo Actual</th>
                          <th>Periodo Anterior</th>
                          <th data-tooltip="Cambio porcentual vs periodo anterior">Delta%</th>
                        </tr>
                      </thead>
                      <tbody>
                        <tr v-for="row in dealsLossRows" :key="row.label">
                          <td class="col-metric">{{ row.label }}</td>
                          <td class="col-count">{{ row.current }}</td>
                          <td class="col-count">{{ row.previous }}</td>
                          <td class="col-delta">
                            <span v-if="Number(row.delta) > 0" class="delta-positive">
                              <span class="material-symbols-outlined">arrow_upward</span>
                              +{{ row.delta }}%
                            </span>
                            <span v-else-if="Number(row.delta) < 0" class="delta-negative">
                              <span class="material-symbols-outlined">arrow_downward</span>
                              {{ row.delta }}%
                            </span>
                            <span v-else class="delta-neutral">
                              <span class="material-symbols-outlined">remove</span>
                              0%
                            </span>
                          </td>
                        </tr>
                        <tr v-if="!dealsLossRows.length">
                          <td colspan="4" style="color:var(--text-muted); text-align:center; padding:1rem;">
                            Sin razones de perdida en el periodo
                          </td>
                        </tr>
                      </tbody>
                    </table>
                  </div>

    Do NOT change any other existing markup. Preserve all surrounding indentation.
  </action>
  <verify>
    <automated>
      grep -n "section.id === 'deals'" "/c/Users/juanc/Documents/anaconda_projects/Pedro - Dev/Index.html"
      # Expect: 2 lines (deals block + dealsLoss block)

      grep -n "dealsReportRows\|dealsLossRows" "/c/Users/juanc/Documents/anaconda_projects/Pedro - Dev/Index.html"
      # Expect: v-for lines referencing both arrays

      grep -n "formatAmount" "/c/Users/juanc/Documents/anaconda_projects/Pedro - Dev/Index.html"
      # Expect: at least 1 new match in deals block (formatAmount already exists in Index.html from embudo)
    </automated>
  </verify>
  <done>
    - Deals funnel table block renders rows from dealsReportRows with Si / No / Monto columns
    - Deals loss-reason block renders rows from dealsLossRows with current / previous / delta columns
    - Both blocks are v-else-if on section.id so they appear inside the existing accordion loop
    - The fallback "Seccion en construccion..." v-else remains intact after the new blocks
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Deploy and browser-verify Deals tables</name>
  <files>App.html, Index.html, Analytics.js</files>
  <action>
    Deploy all three files via clasp push, then verify both Deals accordion sections render
    correctly in the browser with real data from getSDRReport().
  </action>
  <verify>Both "Deals / Negociaciones" and "Deals - Razones de Perdida" sections appear and expand in the Reportes view with no console errors.</verify>
  <done>User confirms both tables render with correct structure and data</done>
  <what-built>
    calculateDealsReport_() is live in Analytics.js. App.html has dealsReportRows and dealsLossRows. Index.html shows both accordion sections (Deals / Negociaciones and Deals - Razones de Perdida).
  </what-built>
  <how-to-verify>
    1. Run: clasp push (from project directory)
    2. Open the GAS web app URL in a browser (ADMIN or GERENTE role).
    3. Navigate to Reportes view.
    4. Select any date range and click "Generar Reporte".
    5. Scroll to the bottom of the accordion — confirm two new sections appear:
       "Deals / Negociaciones" and "Deals - Razones de Perdida".
    6. Expand "Deals / Negociaciones" — confirm rows: Contactado, Cotizado, En Negociacion,
       Asistio Demo, Firmo Contrato, Fondeo with Si/No columns and Monto showing "$X" or "--".
    7. Expand "Deals - Razones de Perdida" — confirm reason rows with delta arrows, OR the
       "Sin razones de perdida" empty-state message if no Perdido deals exist in the period.
    8. Verify no JavaScript console errors. No "Seccion en construccion..." for deals sections.
  </how-to-verify>
  <resume-signal>Type "verified" if both tables render correctly, or describe any issues seen</resume-signal>
</task>

</tasks>

<verification>
Overall phase checks:
- grep -c "dealsReportRows" App.html  should return >= 3 (computed, return, two uses)
- grep -n "section.id === 'deals'" Index.html  should return 2 lines
- No "Seccion en construccion" shown for deals or dealsLoss sections in the browser
- All existing sections (embudoGeneral through perdioVenta) still render correctly after deploy
</verification>

<success_criteria>
- User can see "Deals / Negociaciones" accordion section with Contactado through Fondeo funnel rows, each showing Si count, No count, and Monto where applicable
- User can see "Deals - Razones de Perdida" accordion section with reason labels, periodo actual count, periodo anterior count, and delta % arrows
- Both sections update when the period selector is changed and report is re-generated
- Zero console errors after deploy
</success_criteria>

<output>
After completion, create `.planning/phases/04-deals-backend-+-frontend/04-02-SUMMARY.md`
</output>
