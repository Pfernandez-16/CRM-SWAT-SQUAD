---
phase: 03-toques-semaforos-razones
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Index.html
autonomous: true
requirements:
  - RAZNES-01
  - RAZNES-02
  - RAZNES-03
  - RAZPERD-01
  - RAZPERD-02
  - RAZPERD-03

must_haves:
  truths:
    - "noPasoVentas table renders 6 reason rows + 1 summary row (Total Descartados) with Mix% column"
    - "perdioVenta table renders 13 labeled reason rows + sinEspecificar + 1 summary row (Total Perdidas) = 15 rows with Mix% column"
    - "Mix% bar width is capped at 100% via Math.min — visual bar never overflows cell"
    - "Every reason row shows delta % arrows (up/down/neutral) for all three segments"
    - "Total Descartados and Total Perdidas summary rows display in bold with mixPct = 100.0%"
  artifacts:
    - path: "Index.html"
      provides: "noPasoVentas section (lines 1084-1202) and perdioVenta section (lines 1204-1322) with mixPct bar cap applied"
      contains: "Math.min(Number(row.mixPct), 100)"
    - path: "App.html"
      provides: "razonesNoPasoRows (7 rows) and razonesPerdioRows (15 rows) computed arrays"
      contains: "razonesNoPasoRows, razonesPerdioRows, calcMix"
  key_links:
    - from: "Index.html noPasoVentas percentage-bar"
      to: "row.mixPct"
      via: "':style' binding with Math.min cap"
      pattern: "Math\\.min.*mixPct.*100"
    - from: "Index.html perdioVenta percentage-bar"
      to: "row.mixPct"
      via: "':style' binding with Math.min cap"
      pattern: "Math\\.min.*mixPct.*100"
---

<objective>
Audit the two Razones section templates (noPasoVentas and perdioVenta), apply the one required fix (mixPct bar width cap), and confirm all six RAZNES/RAZPERD requirements are satisfied.

Purpose: The razones tables exist and are wired correctly. The only real code change in Phase 3 is capping the mixPct percentage bar at 100% to prevent visual overflow when reason counts overlap. This is a single :style binding edit applied to both razones tables.
Output: Index.html with Math.min cap applied to both mixPct bars; audit confirming all 6 razones requirements satisfied.
</objective>

<execution_context>
@C:/Users/juanc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/juanc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-toques-semaforos-razones/03-RESEARCH.md

<interfaces>
<!-- Razones computed arrays in App.html — verified at lines 267-305 -->

// razonesNoPasoRows — App.html lines 267-281
// Returns 7 rows: 6 reason rows + 1 summary row
// Row shape: { label: string, metric: segmented_metric, type: 'count', mixPct: string, isSummary?: true }
// mixPct: calcMix(metric) → '34.5' (string, 1 decimal)
// Summary row: { label: 'Total Descartados', ..., isSummary: true, mixPct: '100.0' }
const razonesNoPasoRows = computed(...)  // App.html line 267

// razonesPerdioRows — App.html lines 283-305
// Returns 15 rows: 13 labeled reasons + sinEspecificar + 1 summary row
// Same shape as razonesNoPasoRows. Summary row: { label: 'Total Perdidas', ..., isSummary: true, mixPct: '100.0' }
const razonesPerdioRows = computed(...)  // App.html line 283

// calcMix pattern (both computeds):
// const totalBase = rnp._totalDescartados?.total?.count || 0;
// const calcMix = (metric) => totalBase > 0
//     ? ((metric?.total?.count || 0) / totalBase * 100).toFixed(1)
//     : '0.0';

<!-- The current buggy bar binding (BEFORE fix) in Index.html -->
// Line 1115 (noPasoVentas):
// :style="{ width: row.mixPct + '%', background: 'var(--accent-danger, #ef4444)' }"

// Line 1235 (perdioVenta):
// :style="{ width: row.mixPct + '%', background: 'var(--accent-danger, #ef4444)' }"

<!-- The correct bar binding (AFTER fix) -->
// :style="{ width: Math.min(Number(row.mixPct), 100) + '%', background: 'var(--accent-danger, #ef4444)' }"

<!-- WHY this fix is needed -->
// razonesNoPasoVentas reasons CAN OVERLAP — a lead can match multiple BANT disqualifiers
// simultaneously. Therefore the sum of all reason mixPct values can exceed 100%.
// Individual values can technically exceed 100% if, say, 5 leads are each the only Descartado
// but each matches 2 reasons. The bar must be capped at 100% width to avoid overflow.
// The display text {{ row.mixPct }}% is intentionally NOT capped — shows real computed value.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Audit razones computed arrays in App.html</name>
  <files>App.html</files>
  <action>
    Read-only audit of the razones computed arrays. DO NOT edit App.html.

    STEP 1 — razonesNoPasoRows (App.html lines 267-281):
    - VERIFY: guard condition is `!reportData.value || !reportData.value.razonesNoPasoVentas`
    - VERIFY: 6 reason rows are present:
      noPerfilAdecuado, sinPresupuesto, sinInteresGenuino, necesitaTercero, noEntendioMarketing, otros
    - VERIFY: 7th row is the summary: `{ label: 'Total Descartados', ..., isSummary: true, mixPct: '100.0' }`
    - VERIFY: calcMix divides by `rnp._totalDescartados?.total?.count`
    - VERIFY: calcMix returns '0.0' when totalBase === 0 (zero-division guard)

    STEP 2 — razonesPerdioRows (App.html lines 283-305):
    - VERIFY: guard condition is `!reportData.value || !reportData.value.razonesPerdioVenta`
    - VERIFY: 14 reason rows (13 labeled + sinEspecificar) — count them
    - VERIFY: 15th row is summary: `{ label: 'Total Perdidas', ..., isSummary: true, mixPct: '100.0' }`
    - VERIFY: calcMix divides by `rpv._totalPerdidas?.total?.count`

    STEP 3 — Return block (App.html ~lines 2043-2046):
    - VERIFY: razonesNoPasoRows and razonesPerdioRows are both exported

    Record results: count actual rows found in each computed. Note any gap vs expected (7 and 15 rows respectively).
  </action>
  <verify>
    grep -n "razonesNoPasoRows\|razonesPerdioRows" App.html | head -10
    grep -n "Total Descartados\|Total Perdidas" App.html
    grep -n "calcMix" App.html | head -5

    Expected: razonesNoPasoRows and razonesPerdioRows in return block, calcMix present in both computeds.
  </verify>
  <done>
    Audit confirms: razonesNoPasoRows has 7 rows (6 reasons + Total Descartados), razonesPerdioRows has 15 rows (14 reasons + Total Perdidas). Both computeds have calcMix with zero-division guard. Both symbols exported in return block.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply mixPct bar cap (Math.min) to both razones tables in Index.html</name>
  <files>Index.html</files>
  <action>
    This is the ONE required code change in all of Phase 3. Apply the mixPct bar width cap to prevent visual overflow.

    STEP 1 — Read Index.html lines 1108-1130 (noPasoVentas table body) and lines 1228-1260 (perdioVenta table body) to confirm current state of the :style binding before editing.

    STEP 2 — Locate and fix the noPasoVentas percentage-bar binding:
    Find in Index.html (around line 1115):
      :style="{ width: row.mixPct + '%', background: 'var(--accent-danger, #ef4444)' }"
    Replace with:
      :style="{ width: Math.min(Number(row.mixPct), 100) + '%', background: 'var(--accent-danger, #ef4444)' }"

    CRITICAL: Only change the width expression. Do NOT change:
    - The background color
    - The surrounding div class="percentage-bar"
    - The adjacent span {{ row.mixPct }}% (the display text must still show the real uncapped value)
    - Any other style attributes

    STEP 3 — Locate and fix the perdioVenta percentage-bar binding:
    Find in Index.html (around line 1235):
      :style="{ width: row.mixPct + '%', background: 'var(--accent-danger, #ef4444)' }"
    Replace with:
      :style="{ width: Math.min(Number(row.mixPct), 100) + '%', background: 'var(--accent-danger, #ef4444)' }"

    Same constraint: only change the width expression. Leave everything else unchanged.

    STEP 4 — Verify the fix is applied correctly:
    - Search the file for `Math.min` — should find exactly 2 occurrences (one per table)
    - Confirm `row.mixPct + '%'` no longer appears in the percentage-bar divs (0 occurrences in those blocks)
    - Confirm `{{ row.mixPct }}%` still appears in the percentage-value spans (text unchanged)
  </action>
  <verify>
    grep -n "Math.min" Index.html
    grep -n "width: row.mixPct" Index.html

    Expected: 2 hits for Math.min (one per table). 0 hits for "width: row.mixPct" (both replaced).
  </verify>
  <done>
    Index.html has Math.min(Number(row.mixPct), 100) cap applied to both the noPasoVentas and perdioVenta percentage-bar :style bindings. Display text {{ row.mixPct }}% is unchanged (still shows real value). No other lines modified.
  </done>
</task>

<task type="auto">
  <name>Task 3: Audit razones table markup in Index.html for RAZNES/RAZPERD requirements</name>
  <files>Index.html</files>
  <action>
    Read-only audit of the razones table HTML templates.

    STEP 1 — noPasoVentas section (Index.html lines 1084-1202):
    - VERIFY: section guard is `section.id === 'noPasoVentas'`
    - VERIFY: Mix% column header exists (`<th ... Mix%>` or similar)
    - VERIFY: `v-for="row in razonesNoPasoRows"` is present in tbody
    - VERIFY: Mix% bar td exists (class="col-percentage" with percentage-bar div)
    - VERIFY: delta display exists for 'total' segment (delta-positive / delta-negative / delta-neutral spans)
    - VERIFY: isSummary row style is applied (`row.isSummary ? 'font-weight:700;...' : ''`)
    - VERIFY: Manufacturers and Individuals segment columns are present (colspan="3" headers)

    STEP 2 — perdioVenta section (Index.html lines 1204-1322):
    - Same checklist as noPasoVentas but for `section.id === 'perdioVenta'` and `razonesPerdioRows`

    STEP 3 — Document RAZNES/RAZPERD requirement satisfaction:
    - RAZNES-01: 6 categories mapped (in razonesNoPasoRows — confirmed in Task 1)
    - RAZNES-02: Mix% column present + bar capped (fixed in Task 2)
    - RAZNES-03: Delta % arrows render for all rows (from template delta spans)
    - RAZPERD-01: 13+ categories (14 reasons + summary — confirmed in Task 1)
    - RAZPERD-02: Mix% column present + bar capped (fixed in Task 2)
    - RAZPERD-03: Delta % arrows render for all rows (from template delta spans)
  </action>
  <verify>
    grep -n "noPasoVentas\|perdioVenta" Index.html | head -5
    grep -n "razonesNoPasoRows\|razonesPerdioRows" Index.html | head -5
    grep -n "Mix%" Index.html | head -4

    All three searches must return hits in the razones section blocks.
  </verify>
  <done>
    All 6 RAZNES/RAZPERD requirements audited and confirmed satisfied:
    - RAZNES-01: 6 reason rows confirmed in razonesNoPasoRows computed
    - RAZNES-02: Mix% column in template + bar capped at 100% (fix applied)
    - RAZNES-03: Delta spans present for all rows
    - RAZPERD-01: 14 reason rows (13 labeled + sinEspecificar) confirmed
    - RAZPERD-02: Mix% column + bar capped at 100% (fix applied)
    - RAZPERD-03: Delta spans present for all rows
  </done>
</task>

</tasks>

<verification>
After plan completes:

1. `grep -n "Math.min" Index.html` → exactly 2 lines (both razones table percentage-bar bindings)
2. `grep -n "width: row.mixPct" Index.html` → 0 lines (old unguarded binding removed)
3. `grep -n "row.mixPct }}%" Index.html` → 2+ lines (display text still shows real value)
4. `grep -n "razonesNoPasoRows\|razonesPerdioRows" App.html` → hits in computed definitions AND return block

No other files modified.
</verification>

<success_criteria>
- mixPct bar cap applied to both razones tables: `Math.min(Number(row.mixPct), 100) + '%'`
- Display text `{{ row.mixPct }}%` unchanged (shows real value, may exceed 100)
- All 6 razones requirements audited and confirmed satisfied
- No regressions: other Index.html sections unmodified
</success_criteria>

<output>
After completion, create `.planning/phases/03-toques-semaforos-razones/03-02-SUMMARY.md`
</output>
