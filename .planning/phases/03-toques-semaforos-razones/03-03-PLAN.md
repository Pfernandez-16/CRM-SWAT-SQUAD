---
phase: 03-toques-semaforos-razones
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - .planning/phases/03-toques-semaforos-razones/03-VALIDATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - TOQUES-01
  - TOQUES-02
  - TOQUES-03
  - TOQUES-04
  - TOQUES-05
  - RAZNES-01
  - RAZNES-02
  - RAZNES-03
  - RAZPERD-01
  - RAZPERD-02
  - RAZPERD-03

must_haves:
  truths:
    - "All 6 Phase 3 table sections render visibly in the browser with real data after clicking Generar Reporte"
    - "matrizContactabilidad shows Toque 1-10 as rows with product names as column headers"
    - "Semaforo Contesto shows Telefono, WhatsApp, and Correo rows; Semaforo No Contesto shows only Telefono and WhatsApp"
    - "Razones tables show Mix% bars that do not overflow their cells"
    - "Segment selector on both semaforo grids changes counts in both grids simultaneously"
    - "03-VALIDATION.md is updated to nyquist_compliant: true and status: complete"
    - "All 11 Phase 3 requirements marked complete in REQUIREMENTS.md"
  artifacts:
    - path: ".planning/phases/03-toques-semaforos-razones/03-VALIDATION.md"
      provides: "Phase 3 validation sign-off"
      contains: "nyquist_compliant: true"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability for all 11 Phase 3 requirements"
      contains: "TOQUES-01.*Complete|RAZPERD-03.*Complete"
  key_links:
    - from: "03-VALIDATION.md"
      to: "browser acceptance checklist"
      via: "manual verification of all 5 success criteria"
      pattern: "nyquist_compliant: true"
---

<objective>
Deploy the Phase 3 changes (mixPct cap from plan 02) via clasp push, run the cross-section integration verification, confirm all 6 table sections render correctly with real data, and close out Phase 3 by updating VALIDATION.md and REQUIREMENTS.md.

Purpose: Phase 3 produces no new features beyond what already existed — the primary deliverable is confirmed-working report sections. This plan locks in that confirmation and marks all 11 requirements complete.
Output: 03-VALIDATION.md signed off, 11 requirements marked complete in REQUIREMENTS.md, Phase 3 done.
</objective>

<execution_context>
@C:/Users/juanc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/juanc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-toques-semaforos-razones/03-RESEARCH.md
@.planning/phases/03-toques-semaforos-razones/03-01-SUMMARY.md
@.planning/phases/03-toques-semaforos-razones/03-02-SUMMARY.md

<interfaces>
<!-- Phase 3 success criteria to verify (from ROADMAP.md) -->
// SC1: Contactability matrix renders Toque 1-10 as rows; product/country names as dynamic columns
// SC2: Both semaphore grids render — Contesto: 3 channels (incl. Correo); NoContesto: 2 channels (excl. Correo)
//      Segment selector changes counts in BOTH grids simultaneously
// SC3: Sin Respuesta indicator renders with count + delta for all 3 segments
// SC4: Razones No Pasó a Ventas: 6 reason rows + 1 summary, Mix% column, delta arrows, summary row bold
// SC5: Razones Perdió la Venta: 14 reason rows + 1 summary, Mix% column, delta arrows, summary row bold

<!-- Grep audit checks from 03-VALIDATION.md -->
// grep -n "matrizContactabilidad" Index.html      → must match section guard
// grep -n "semaforoContestoRows\|semaforoNoContestoRows" App.html  → legacy name check (expected 0 hits)
// grep -n "semaforoContestoGrid\|semaforoNoContestoGrid" App.html  → correct names
// grep -n "sinRespuestaRows" App.html             → must match
// grep -n "noPasoVentasRows" App.html             → legacy name check (expected 0 hits, name is razonesNoPasoRows)
// grep -n "razonesNoPasoRows\|razonesPerdioRows" App.html → correct names
// grep -n "Math.min" Index.html                   → must return 2 hits (mixPct cap fix)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Static grep audit — confirm all Phase 3 symbols and the mixPct fix are deployed</name>
  <files>.planning/phases/03-toques-semaforos-razones/03-VALIDATION.md</files>
  <action>
    Run static grep checks against App.html and Index.html to confirm Phase 3 code is in place before triggering any browser verification. This mirrors the pattern used in Phase 2 plan 02-02.

    Run these checks and record results:

    CHECK 1 — matrizContactabilidad section guard:
      grep -n "matrizContactabilidad" Index.html
      Expected: at least 1 hit (section v-else-if guard)

    CHECK 2 — Semaforo computed names in App.html:
      grep -n "semaforoContestoGrid\|semaforoNoContestoGrid" App.html
      Expected: at least 4 hits (2 computed definitions + 2 in return block)

    CHECK 3 — sinRespuestaRows in App.html:
      grep -n "sinRespuestaRows" App.html
      Expected: at least 2 hits (computed definition + return block)

    CHECK 4 — razonesNoPasoRows and razonesPerdioRows in App.html:
      grep -n "razonesNoPasoRows\|razonesPerdioRows" App.html
      Expected: at least 4 hits (2 computed definitions + 2 in return block)

    CHECK 5 — mixPct bar cap applied in Index.html:
      grep -n "Math.min" Index.html
      Expected: exactly 2 hits (one per razones table)

    CHECK 6 — old unguarded bar binding gone from Index.html:
      grep -n "width: row.mixPct" Index.html
      Expected: 0 hits

    CHECK 7 — semaforoSegment shared ref in App.html:
      grep -n "semaforoSegment" App.html
      Expected: at least 2 hits (definition + return block)

    If ANY check fails: document the failure with exact output and stop. Do NOT update VALIDATION.md to passing. Do NOT proceed to Task 2.

    If ALL checks pass: update 03-VALIDATION.md — add a Grep Audit Results table (format from 02-VALIDATION.md) and set `wave_0_complete: true` in frontmatter.
  </action>
  <verify>
    All 7 grep checks pass (expected hit counts met).
    03-VALIDATION.md grep audit results table added.
  </verify>
  <done>
    All 7 static checks pass. 03-VALIDATION.md updated with grep results table and wave_0_complete: true. No unexpected mismatches found.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All six Phase 3 report sections (matrizContactabilidad, semaforoContesto, semaforoNoContesto, sinRespuesta, noPasoVentas, perdioVenta) are implemented and have been statically verified. One code fix was applied: mixPct bar width cap (Math.min) on both razones tables.

    Changes deployed: clasp push was run in Plan 02 (or needs to be run now — push to GAS before testing).
  </what-built>
  <how-to-verify>
    1. Open the GAS web app URL → navigate to Reportes view
    2. Set a date range with real data and click "Generar Reporte"
    3. Expand "Matriz de Contactabilidad":
       - Rows are labeled Toque 1 through Toque 10 (or however many exist in the data)
       - Column headers are product names (default) — toggle to "País" and verify country names appear
       - Each cell shows a non-negative integer (raw count, no delta column)
    4. Expand "Semáforo Contestó":
       - 3 rows: Telefono, WhatsApp, Correo
       - 4 toque columns; Telefono and WhatsApp show -- in Toque 4 (they only have 3 toques)
       - Correo shows a count in Toque 4
    5. Expand "Semáforo No Contestó":
       - Only 2 rows: Telefono and WhatsApp (NO Correo row)
       - 3 toque columns
       - Change the segment selector — both semaforo grids update together
    6. Expand "Sin Respuesta 6to Toque":
       - Single row with Total / Manufacturers / Individuals counts and delta arrows
    7. Expand "Por qué no pasó a Ventas":
       - 6 reason rows + 1 bold Total Descartados summary row (7 rows total)
       - Mix% column with red bars that fit within their cell (no overflow)
       - Total Descartados shows 100.0%
       - Delta arrows on all rows
    8. Expand "Por qué perdió la venta":
       - 14 reason rows + 1 bold Total Perdidas summary row (15 rows total)
       - Same Mix% column and delta arrow pattern
       - Total Perdidas shows 100.0%

    All 5 Phase 3 success criteria must be satisfied.
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 steps pass. Describe any specific item that fails.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Close out Phase 3 — update VALIDATION.md and mark requirements complete</name>
  <files>
    .planning/phases/03-toques-semaforos-razones/03-VALIDATION.md
    .planning/REQUIREMENTS.md
  </files>
  <action>
    Only run after Task 2 checkpoint is approved.

    STEP 1 — Update 03-VALIDATION.md:
    - Set frontmatter: `nyquist_compliant: true`, `status: complete`
    - Add Manual Acceptance Checklist results under "Validation Sign-Off":
      - [x] All 6 section computeds confirmed in App.html
      - [x] mixPct cap (Math.min 100) applied
      - [x] Browser: all 6 table sections render
      - [x] nyquist_compliant: true set
    - Add Approval line: "Approved: [today's date]"

    STEP 2 — Update REQUIREMENTS.md traceability table:
    Change status from "Pending" to "Complete" for all 11 Phase 3 requirements:
      - TOQUES-01, TOQUES-02, TOQUES-03, TOQUES-04, TOQUES-05
      - RAZNES-01, RAZNES-02, RAZNES-03
      - RAZPERD-01, RAZPERD-02, RAZPERD-03

    STEP 3 — Update ROADMAP.md Phase 3 entry:
    - Change `- [ ] **Phase 3: Toques + Semáforos + Razones**` to `- [x] **Phase 3: Toques + Semáforos + Razones**`
    - Add completion date in parentheses: `(completed [today's date])`
    - Change `**Plans**: TBD` to `**Plans**: 3 plans`
    - Update the Plans list to checked:
      - [x] 03-01-PLAN.md — Audit Toques + Semáforos sections
      - [x] 03-02-PLAN.md — Audit Razones tables + apply mixPct cap
      - [x] 03-03-PLAN.md — Deploy, cross-section integration verify, phase sign-off

    Only modify the Phase 3 section of ROADMAP.md. Do not touch Phase 4.
  </action>
  <verify>
    grep -n "nyquist_compliant: true" .planning/phases/03-toques-semaforos-razones/03-VALIDATION.md
    grep -n "TOQUES-01.*Complete\|RAZPERD-03.*Complete" .planning/REQUIREMENTS.md

    Both searches must return hits.
  </verify>
  <done>
    03-VALIDATION.md: nyquist_compliant: true, status: complete, all 4 sign-off checkboxes checked.
    REQUIREMENTS.md: all 11 Phase 3 requirements show "Complete" status.
    ROADMAP.md: Phase 3 marked [x] with completion date.
    Phase 3 is fully closed.
  </done>
</task>

</tasks>

<verification>
Phase 3 complete when:
1. Static grep audit passes all 7 checks (Task 1)
2. Human approves all 8 browser verification steps (Task 2 checkpoint)
3. 03-VALIDATION.md has `nyquist_compliant: true` and `status: complete`
4. REQUIREMENTS.md shows all 11 Phase 3 requirements as Complete
5. ROADMAP.md Phase 3 entry is checked [x] with completion date
</verification>

<success_criteria>
- All 11 Phase 3 requirements (TOQUES-01..05, RAZNES-01..03, RAZPERD-01..03) marked Complete
- 03-VALIDATION.md signed off with nyquist_compliant: true
- ROADMAP.md Phase 3 entry checked [x]
- No regressions: Phase 2 tables still render correctly (visible in same report view)
- Phase 4 (Deals) can begin immediately — no blockers from Phase 3
</success_criteria>

<output>
After completion, create `.planning/phases/03-toques-semaforos-razones/03-03-SUMMARY.md`
</output>
